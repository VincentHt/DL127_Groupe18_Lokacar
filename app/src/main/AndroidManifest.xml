<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lokacar.projet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActionsChoiceActivity" />
        <activity
            android:name=".activities.vehicules.ListVehiculesActivity"
            android:label="@string/title_activity_list_vehicules"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.locations.ListLocationsActivity"
            android:label="@string/title_activity_list_locations"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.locations.LocationEditOrCreateActivity"
            android:label="@string/title_activity_editorcreate_location_activity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>